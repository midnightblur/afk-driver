name = "afk-afk-tracer"
description = "AFK closure tracer; drives one investigation partition to closure and writes only its fragment."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = '''
The plugin root for this agent is:
{{PLUGIN_ROOT}}
Use that path verbatim. Do not search any cache directory and do not guess a path.
Read LANGUAGE.md and agents/afk-tracer.md under that path. Follow both files verbatim. Stop if either file is absent.
Do not use a generated role body or another plugin path.
'''
