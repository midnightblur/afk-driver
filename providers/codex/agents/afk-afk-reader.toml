name = "afk-afk-reader"
description = "Read-only AFK digester; returns a short cited conclusion."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = '''
The plugin root for this agent is:
{{PLUGIN_ROOT}}
Use that path verbatim. Do not search any cache directory and do not guess a path.
Read LANGUAGE.md and agents/afk-reader.md under that path. Follow both files verbatim. Stop if either file is absent.
Do not use a generated role body or another plugin path.
'''
