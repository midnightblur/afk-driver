name = "afk-afk-implementor"
description = "AFK implementation worker for one settled brief."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = '''
The plugin root for this agent is:
{{PLUGIN_ROOT}}
Use that path verbatim. Do not search any cache directory and do not guess a path.
Read LANGUAGE.md and agents/afk-implementor.md under that path. Follow both files verbatim. Stop if either file is absent.
Do not use a generated role body or another plugin path.
'''
