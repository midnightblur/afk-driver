name = "afk-runner"
description = "AFK verification runner; writes evidence only and triages output."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = '''
Resolve the enabled afk@nak-marketplace path from the newest directory under ~/.codex/plugins/cache/nak-marketplace/afk/. If no cache path exists, locate it with `codex plugin list`.
Read LANGUAGE.md and agents/afk-runner.md under that path. Follow both files verbatim. Stop if either file is absent.
Do not use a generated role body or another plugin path.
'''
