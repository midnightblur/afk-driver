# Native-contract prose exceptions. One tab-separated entry per exception:
#   plugin-relative path glob<TAB>rule<TAB>line regex<TAB>reason
#
# Rules: claude-project-dir, claude-plugin-root, harness-env, harness-name,
# harness-tool, mcp-prefix. Keep patterns line-specific; broad file waivers hide
# new coupling in the same file.
LAVISH.md	harness-name	lavish-axi setup hooks.*Claude Code, Codex, OpenCode, GitHub Copilot CLI	Third-party command capability names its supported runtimes.
CLAUDE.md	claude-project-dir	Give `CLAUDE_PROJECT_DIR` and `PLUGIN_ROOT` a fallback	Binding steering names the dependency it forbids.
CLAUDE.md	claude-plugin-root	Give `CLAUDE_PROJECT_DIR` and `PLUGIN_ROOT` a fallback	Binding steering names the dependency it forbids.
CHANGELOG.md	harness-name	on Codex machines\) so every agent session	Historical shipped behavior.
CHANGELOG.md	harness-name	The distribution law is written doctrine.*Claude.*Codex parity	Historical shipped behavior.
CHANGELOG.md	harness-name	Codex now has the same opt-in boundary	Historical shipped behavior.
CHANGELOG.md	harness-name	/afk-toolkit:understand.*live Claude Code session	Historical shipped behavior.
CHANGELOG.md	harness-name	Model selection goes role-based.*Codex CLI equivalents	Historical shipped behavior.
CHANGELOG.md	harness-name	Dual-provider support:.*OpenAI Codex CLI	Historical shipped behavior.
CHANGELOG.md	claude-project-dir	setup.*registered.*CLAUDE_PROJECT_DIR	Historical shipped behavior.
# /afk-toolkit:setup's register and driver provision one machine per harness: section O
# provisions a named CLI, so its entries name it. Keep each entry pinned to the
# provisioning line it covers — never a whole-file waiver.
skills/afk/setup/MANIFEST.md	harness-name	only when Codex \(O1\) is installed	Opt-in steering fix names the harness whose steering file it appends to.
skills/afk/setup/MANIFEST.md	harness-name	^## O — OpenAI Codex CLI	Register section header names the harness the section provisions.
skills/afk/setup/MANIFEST.md	harness-name	deferred \(Codex not installed\)	Section gating rule names the harness whose absence defers it.
skills/afk/setup/MANIFEST.md	harness-name	running the native plugin under Codex CLI	Entry states which harness the dependency serves.
skills/afk/setup/MANIFEST.md	harness-name	install or update Codex CLI	Human fix instruction names the CLI to install.
skills/afk/setup/MANIFEST.md	harness-name	^### O5 · Codex agent TOML stubs	Entry title names the harness whose stub format it provisions.
skills/afk/setup/MANIFEST.md	claude-plugin-root	`PLUGIN_ROOT` / `PLUGIN_DATA` \| `hooks/lib/providers/codex.sh`	Env-toggle index lists the variable names one adapter reads.
skills/afk/setup/SKILL.md	harness-name	Section \*\*O\*\* \(OpenAI Codex CLI\)	Driver names the register section it gates.
skills/afk/setup/SKILL.md	harness-name	deferred \(Codex not installed\)	Driver states the gated section's wholesale verdict.
CHANGELOG.md	harness-name	tier names: `PROVIDERS.md`. On Codex the	Historical shipped behavior.
# The Codex agent stubs carry an install-time placeholder literally spelled
# {{PLUGIN_ROOT}}. It is a token this plugin substitutes, not a harness variable
# the plugin reads, so the prose that documents it names it.
FRESHNESS.md	claude-plugin-root	`/afk-toolkit:setup` copies them and resolves	Stub-freshness row names the placeholder setup substitutes.
skills/afk/setup/MANIFEST.md	claude-plugin-root	its `\{\{PLUGIN_ROOT\}\}` placeholder is	Probe names the placeholder it checks for.
skills/afk/setup/MANIFEST.md	claude-plugin-root	replace every `\{\{PLUGIN_ROOT\}\}` occurrence with that root	Fix names the placeholder it substitutes.
skills/afk/setup/MANIFEST.md	claude-plugin-root	destination contains no `\{\{PLUGIN_ROOT\}\}`	Post-condition names the placeholder that must be gone.
skills/afk/setup/MANIFEST.md	harness-name	installed plugin root that Codex plugin metadata reports	Entry states which harness's metadata supplies the root.
skills/afk/setup/MANIFEST.md	harness-name	`Installed plugin root:` line of `codex plugin list`	Fix names the CLI whose output carries the root.
skills/afk/setup/MANIFEST.md	harness-name	`\[plugins\."afk-toolkit@afk-toolkit"\]` entry in `~/.codex/config.toml`	Fix names the harness config file that records the root.
skills/afk/setup/MANIFEST.md	harness-name	read the installed plugin root from Codex plugin metadata	Fix names the harness whose metadata supplies the root.
CHANGELOG.md	harness-name	installable from GitHub on Claude Code	Release entry states which harnesses the 1.0.0 install was proven on.
CHANGELOG.md	harness-name	^  and Codex CLI\.\*\* It was a directory	Same release entry, continued on the next line.

# The 1.0.1 fix IS a difference between two harnesses: one expands a
# placeholder in an MCP registration and the other does not. A changelog entry
# that would not say which is which cannot be acted on by the reader it is for.
CHANGELOG.md	harness-name	^- \*\*The tracker MCP server never started on Codex CLI\.\*\* Codex expands neither	Release entry names the harness whose behaviour the defect depended on.
CHANGELOG.md	claude-plugin-root	`\$\{PLUGIN_ROOT\}` nor `\$\{CLAUDE_PLUGIN_ROOT\}` inside an `args` entry	Entry names the two placeholders that were not expanded.
CHANGELOG.md	harness-name	tools, while Claude Code — which does expand it — worked	Same entry names the harness where the defect did not appear.

# A "which versions are safe" notice is useless without the harness name: the
# defect it warns about exists on one harness and not the other.
CHANGELOG.md	harness-name	^- \*\*If you are on Codex CLI, 1\.0\.0 does not work	Compatibility notice names the affected harness.
CHANGELOG.md	harness-name	at all and every skill that reads or writes a work item failed\. Claude Code is	Same notice names the unaffected harness.
