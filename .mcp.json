{
  "mcpServers": {
    "jira": {
      "command": "python",
      "args": [
        "-c",
        "import glob, json, os, re, runpy, sys\nrel = os.path.join(\"mcp-servers\", \"jira\", \"server.py\")\nhome = os.path.expanduser(\"~\")\nroots = [a for a in sys.argv[1:] if a and \"${\" not in a]\nroots += [os.environ.get(\"CLAUDE_PLUGIN_ROOT\"), os.environ.get(\"PLUGIN_ROOT\")]\nroots.append(os.path.join(os.getcwd(), \"tools\", \"payable\", \"ai-agents\", \"plugins\", \"workflow\"))\nfor pat in (\"*\", \".*\"):\n    roots += sorted(glob.glob(os.path.join(home, pat, \"plugins\", \"cache\", \"*\", \"afk\", \"*\")), reverse=True)\nkeys = (\"JIRA_BASE_URL\", \"JIRA_EMAIL\", \"JIRA_API_TOKEN\", \"JIRA_DEFAULT_PROJECT\")\ndef adopt(env):\n    for k in keys:\n        v = env.get(k)\n        if isinstance(v, str) and v and not os.environ.get(k):\n            os.environ[k] = v\ndef missing():\n    return not all(os.environ.get(k) for k in keys[:3])\nif missing():\n    try:\n        stack = [json.load(open(os.path.join(home, \".claude.json\"), encoding=\"utf-8\"))]\n    except Exception:\n        stack = []\n    while stack:\n        node = stack.pop()\n        if isinstance(node, dict):\n            servers = node.get(\"mcpServers\")\n            if isinstance(servers, dict):\n                for name, spec in servers.items():\n                    if \"jira\" in name and isinstance(spec, dict) and isinstance(spec.get(\"env\"), dict):\n                        adopt(spec[\"env\"])\n            stack += [v for v in node.values() if isinstance(v, (dict, list))]\n        elif isinstance(node, list):\n            stack += [v for v in node if isinstance(v, (dict, list))]\nif missing():\n    try:\n        text = open(os.path.join(home, \".codex\", \"config.toml\"), encoding=\"utf-8\").read()\n    except Exception:\n        text = \"\"\n    block = re.search(r\"(?ms)^\\[mcp_servers\\.[A-Za-z0-9_.-]*jira[A-Za-z0-9_.-]*\\.env\\]\\s*$(.*?)(?=^\\[|\\Z)\", text)\n    if block:\n        adopt(dict(re.findall(r\"(?m)^\\s*([A-Z_]+)\\s*=\\s*[\\\"']([^\\\"']*)[\\\"']\", block.group(1))))\nfor root in roots:\n    if root and os.path.isfile(os.path.join(root, rel)):\n        runpy.run_path(os.path.join(root, rel), run_name=\"__main__\")\n        sys.exit(0)\nsys.exit(\"afk jira MCP: mcp-servers/jira/server.py not found. Pass the plugin root as an argument, or export CLAUDE_PLUGIN_ROOT or PLUGIN_ROOT.\")",
        "${CLAUDE_PLUGIN_ROOT}"
      ]
    }
  }
}
