# Grill log — agent-team-topology

Checkpoint of interactive grill sessions. Working state, updated as decisions lock;
superseded when the synthesized artifacts (PRD / SDD / VERIFICATION-PLAN) land.

## Requirements grill

- Ledger: Codex account and quota metadata can be read without a generation turn → pending (FACTS-codex C071, C072 inferred, C077 unverified)
- Ledger: Claude has no quota check that avoids a generation turn → pending (FACTS-codex C073–C075)
- Ledger: 1DevTool `stop --team --close-terminals` closes a team and its terminals → pending (FACTS-codex C037; source-read only, no live trial)
- Ledger: 1DevTool headless run can be spawned and killed by timeout → verified (FACTS-codex C042–C045, EVIDENCE-codex-live.json)
- Ledger: herdr `agent rename` sets only the command handle; the sidebar shows the pane label → verified (this session: `herdr pane rename` fixed the sidebar)
- Ledger: an unattended Claude spawn stops on first-run dialogs (MCP approval, Chrome extension) → verified (this session, pane w5:p2 start)
- Ledger: a headless close kills every process the agent started, on Windows → pending (FACTS-codex C044 covers only the observed process set)
- Ledger: one Fable 5.1 debater used about 1.3% of the Claude 5-hour window per minute; all Claude sessions on one account share that window, the contact agent included → verified (this session, debater-fable status line 79% → 83% in about 3 minutes)
- Ledger: Claude Code exposes the usage-window percent and reset time to a script → verified (research/FACTS-quota.md QH02, QH09; status-line input, only after the first provider response, QH10)
- Ledger: Claude interactive sessions continue on their own at reset; headless, background, teammate and over-24-hour waits do not → verified (research/FACTS-quota.md QH14, QH15)
- Ledger: Codex exposes rate-limit windows and reset times without a generation turn → partial (research/FACTS-quota.md QC02–QC04 documented; QC06 live call untested)
- Ledger: a mechanism exists that wakes or restarts a parked agent at a set time with no human → partial (research/FACTS-quota.md QW01–QW06: session cron needs the session alive; Desktop and cloud schedules persist, cloud uses a fresh clone; this session's background sleep script is still running)
- Ledger: the Claude reset time is a fixed Unix timestamp per window (five-hour, seven-day) → verified (research/FACTS-quota.md QH09). Corrects an earlier row that called it a rolling estimate; the observed jump from 2h 6m to 49m about 15 minutes apart has an unknown cause (not verified: a different window shown, or a misread)
- Ledger: a background shell script can park a herdr agent and prompt it after a wait, with no human → verified (this session: the script woke at 15:43 after the Claude reset, prompted debater-fable, and it wrote research/VERDICT-claude.md; the usage bar then read 0%)
- Ledger: an idle agent's status-line usage is stale — it refreshes only after a model response, so a wake rule must use the stored reset timestamp, not a live percent → verified (this session: debater-fable showed "91%, resets in 49m" unchanged from 14:52 to 15:26; research/FACTS-quota.md QH10)
- Ledger: a headless close kills every process the agent started, on Windows → **REFUTED** (live trial 2026-09-17). A headless run was told to start a detached child, which started a grandchild. After the parent exited with status 0, the child and the grandchild were both still running and had to be killed by hand. Supersedes the earlier `pending` row. Nothing cascades: a parent's exit is not cleanup.
- Ledger: 1DevTool `stop --team --close-terminals` closes a team and its terminals → **UNTESTABLE HERE** (2026-09-17). The command is not on this machine's PATH and the desktop tool's connection is down, so no team could be started or stopped. Not simulated and not inferred from source.
- Ledger: Codex exposes rate-limit windows and reset times without a generation turn → **VERIFIED BY CONTRACT, NOT BY MEASUREMENT** (2026-09-17). The provider's own cached tool declaration on this machine names the fields and states the read costs no credits. No before-and-after token measurement was taken. Nothing in the design rests on it any more (R-5 AUD2 deleted the quota check).
- Ledger: Claude has no quota check that avoids a generation turn → **VERIFIED** (2026-09-17). No usage, quota or status subcommand exists; the auth status output carries no window or reset field; local state holds a static plan label and historical counts only. Confirms the earlier note: the live figure reaches a script only through the status line, and only after a first model response.
- Staples: none — this repository has no STAPLES.md
- Settled: planners debate to reasoned AGREE or DISAGREE-FINAL; only DISAGREE-FINAL points and human-only values reach the human (human, R-1)
- Settled: on a provider usage limit, agents detect it, learn the reset time and resume on their own (human, R-1)
- Settled: terms T1 contact agent, T2 team topology, T3 multi-model, T4 transport, T5 team pattern, T6 lifespan, T7 knowledge store — accepted as worded (human, R-1)
- Settled: the knowledge store is NOT durable and is NOT committed. It serves the workflow's own agents only (a planner's finding feeding a later builder) and is dropped after delivery; the durable record stays PRD, SDD and ADRs (human, R-1, T7 + K1). Overrides the debaters' AG3 catalog-and-keep position.
- Settled: no strict single-agent mode. Subagents are promoted, not restricted: the team topology governs only the main roles, and every agent may fan out native subagents of its own harness to protect its context (human, R-1, D1)
- Settled: C1 today's behaviour with no config, C2 four combinations, C4 role-and-model naming plus pane label — accepted (human, R-1)
- Settled: when live detection and config conflict, ask the human to confirm and fix the config; never silently prefer one (human, R-1, C3)
- Settled: spawned agents run at maximum freedom — `yolo` for Codex, `dangerously-skip-permissions` for Claude (human, R-1, C5). Supersedes the earlier auto-mode steer.
- Settled: AG4 verified-versus-fresh states, AG5 first delivery, AG6 hierarchy deferred, AG7 the detect-park-wake-resume sequence — accepted (human, R-1)
- Settled: unattended wait horizon is 5 hours, the Claude session window; Codex has no 5-hour window, only a weekly one; fetch the exact reset where the provider exposes it (human, R-1, L1)
- Settled: on a usage limit, fall back where a fallback exists, and park until reset only where it does not; extra credit spend is unlikely (human, R-1, L2)
- Settled: the plugin may register an OS scheduled task, on condition that every process it starts is killed cleanly, with no stranded process when work ends or fails (human, R-1, L3)
- Settled: a role writes a running checkpoint as it works. It is what a replacement reads, because the dying model cannot be asked for a handoff — that request is the one the exhausted provider refuses (human, R-2, H1)
- Settled: no provider availability check at all. Failures are reacted to, never predicted (human, R-2, H2). Closes the reopened AG1; the debaters' metadata-only probe is dropped.
- Settled: a single-model role continues on an equivalent model from another provider when its own fails; a role whose independence depends on its model, such as a planner in a debate, waits instead, or the human continues without it (human, R-2, H3). Closes the reopened AG2.
- Settled: equivalence comes from the existing model-tier map, overridable per role (human, R-2, H4)
- Settled: a team pattern declares its own talk edges. Direct messages are allowed only where the pattern grants them in writing; everything else relays through the contact agent (human, R-2, B1)
- Settled: agents filling one role on different models never see each other's work before their own is written. Drafts, critiques and verdicts are recorded per agent with reasons, and agreement is reported per agent, never as one voice (human, R-2, B2)
- Settled: the knowledge store lives in an ignored path beside the feature spec, is never committed, and is deleted with the feature's other run artifacts (human, R-2, S3). Closes the reopened AG3; the repository-wide catalog is dropped.
- Settled: nothing survives the store's deletion. Harvestable facts are already carried by the terminal sync-harness subtask; facts needed only for implementation are dropped, and a later feature rediscovers what it needs (human, R-2, S4)
- Settled: at task start the contact agent proposes a pattern, prices it in agents and relayed messages, and offers a one-off pattern instead. The human picks; it never picks silently (human, R-2, P1)
- Settled: fan-out is NOT a pattern. Calling subagents is every agent's own business, subagents included, decided by the goal and by whether the work would pollute its context (human, R-3, PAT1)
- Settled: any work of the deep-research, grounding, design, planning, review or audit kind uses the debate pattern, two agents or more — inside the afk workflow or outside it (human, R-3, PAT1)
- Settled: lighter work, such as bug fixes and small enhancements, can be covered end to end by a fuller pattern: one contact, one manager or orchestrator, several planners, several reviewers, one builder (human, R-3, PAT1)
- Settled: a pattern is data — roles, model per role, lifespan per role, talk edges — so a user-written pattern has the same power as a built-in one; and a pattern may run once without being saved (human, R-3, PAT2 and PAT3)
- Settled: every agent works in the same directory, or at least a subdirectory of the contact agent's. One worktree is created from the main checkout for the work; no worktree per agent, because the repository may be large (human, R-3, PERM1)
- Settled: no limit is enforced on a spawned agent. What it may do is its harness's business (human, R-3, PERM1). Replaces the workspace-as-boundary recommendation; the shared worktree is a layout choice, not a guard.
- Settled: no spawned agent commits, pushes, opens a change request, or writes to the tracker (human, R-3, PERM2)
- Settled: the run manifest records every agent AND every background process an agent starts; cleanup kills them all and reports what it could not (human, R-3, PERM3)
- Settled: setup always offers the herdr install, not only when team topology is enabled (human, R-3, ENV1)
- Settled: detection reports what it found and never switches transport or model by itself. It must detect the session the human and agents are actually in, not the machine in general — herdr and 1DevTool can both be installed at once (human, R-3, ENV2)
- Settled: with no multi-agent transport, a pattern runs on the harness's own subagents and says so in its report (human, R-3, ENV3)
- Settled: nothing is installed, scheduled, or opened outside the repository without asking first, and every such change is undoable by one command (human, R-3, ENV4)
- Settled: only the contact agent starts and stops team agents; a spawned agent starts and closes its own harness subagents but never a team agent (human, R-4, ACC1). Human note: the contact agent is not necessarily the talk hub — a pattern may spawn an orchestrator and two planners, and the orchestrator, not the contact agent, talks to the planners. Spawning authority and talk hub are separate; the hub question goes to R-5.
- Settled: the knowledge store exists so a later stage can use what an earlier stage learned — a builder reads what the planners knew, a reviewer reads what the builder knew. Within one stage, and above all in a debate, an agent's work is shared only once that agent has finished its own independent work (human, R-4, ACC2 write-in). Replaces the role-blind option; the mechanism is open and goes to R-5.
- Settled: a spawned agent is given a written brief, never the transcript of the human conversation; what it needs is quoted into the brief (human, R-4, ACC3). Their words: that's the point, we don't want to spend tokens.
- Settled: no agent writes the repository config. An agent proposes a pattern; only the human saves it, and a one-off pattern is never written down (human, R-4, ACC4)
- Settled: every store entry carries its author, role, model, time and what it rests on. A claim with no author and no ground is refused, not stored (human, R-4, ACC5)
- Settled: a pattern that asks for a role the environment cannot fill is REFUSED at start — name the role, name what is missing, stop. No reduced start, and no offer round (human, R-4, LIM1). Note: this collides with H2 (no availability check at all) — refusing at start requires knowing at start. Reopens H2 in R-5.
- Settled: a phase is not reported as finished while the run manifest holds a process the plugin could not kill; it names the process, where it is, and what to run (human, R-4, LIM2)
- Settled: an agent closed before its work is written leaves a checkpoint marked unfinished, and a later reader is told so and never reads it as a finding (human, R-4, LIM3)
- Settled: a spawned agent MAY commit and MAY push. Keeping local and remote in sync is worth more than the ban, work happens on feature branches, and merging is the guard — only the human merges (human, R-5, AUD1 write-in). Overrides PERM2 (R-4), which forbade both. Open in R-6: whether a spawned agent may open the change request.
- Settled: check authentication only, once per provider a pattern names, before the first spawn. H2 narrows to usage limits: no quota prediction, because the human knows their own quota (human, R-5, AUD2 write-in). Makes LIM1's refuse-at-start buildable.
- Settled: the wait horizon is per provider window. Beyond it, park with a dated report and tell the human — never drop to a reduced shape, never abandon the run (human, R-5, AUD3). Resolves L1 versus H3 for a Codex weekly reset.
- Settled: each role owns an output path, named in the pattern and repeated in its brief. An agent writes there and in files its brief names. Changing shared git state — checkout, reset, branch switch — belongs to the contact agent alone. A written convention, not an enforced guard (human, R-5, AUD4)
- Settled: a store entry is born unfinished at every write, and only the agent's own clean exit stamps it finished. Peers in the same stage read finished entries only; later stages read everything finished (human, R-5, KNW1). Supplies the mechanism ACC2 left open, and inverts LIM3's stamp so a killed agent needs to write nothing.
- Settled: a pattern may name a role other than the contact agent as the hub for a group of roles — an orchestrator talking to its planners. Starting and closing those agents stays with the contact agent, so one manifest holds everything (human, R-5, KNW2). Writes up the ACC1 note.
- Settled: before the store is deleted, the terminal sync-harness subtask promotes environment facts into a durable register; feature facts are dropped (human, R-5, KNW3). Human condition: do not clutter the harness — only qualified harvestable facts are promoted.
- Settled: the run manifest, every agent name and every scheduled task carry the feature id and the contact session that owns them; a new contact agent adopts an existing manifest by that key (human, R-5, VIS1)
- Settled: the manifest carries each agent's role, model, state and park-until time, and the read-only dashboard reads it, so a parked team never looks like a hung one (human, R-5, VIS3)
- Settled: on a detection-versus-config conflict the plugin asks the human when they are present, and unattended it parks and names the conflict rather than waiting on a prompt nobody will answer (human, R-5, VIS4). Splits C3 by presence.
- Settled: no cost estimate. A fixed slice of each provider window is reserved for the contact agent, and patterns spend the rest — so the agent the human talks to can always answer, with nothing to predict (human, R-6, VIS2B). Closes the R-5 VIS2 card, which the human sent back for a clearer explanation; the estimate I recommended there is dropped because a confident wrong number before approval is worse than no number.
- Settled: a spawned agent may commit and push its own branch. Opening the change request stays with the contact agent, and merging stays with the human (human, R-6, PUSH1)
- Settled: all agents work in ONE worktree — one per feature or bug, never one per agent (human, R-6, PUSH1 note). Restates PERM1; recorded because the human raised it unprompted, so it is the point they most want not to be got wrong.
- Superseded: VIS2, what a pattern is priced in — human asked for a better explanation ("explain better. I don't get it"). Re-asked in R-6.
- Resolved: the lavish false-positive occlusion warnings were the sticky send bar, which sits inside the audited subtree and hit-tests as an opaque blocker over any text scrolled beneath it. Bar dropped a hair below opaque with a backdrop blur; the warning stopped firing on the next poll. Shipped with the draft-persistence fix in PR #24, issue #23.
- Settled: every unproven ledger claim gets a live trial before the PRD — all four, not only the two still load-bearing (human, R-7, LED1). Overrides my recommendation to drop the two quota claims that AUD2 made irrelevant.
- Settled: eleven terms graduate to the plugin GLOSSARY.md — contact agent, team topology, multi-model, transport, team pattern, lifespan, knowledge store, debate pattern, run manifest, running checkpoint, talk edges. This session's nicknames and pane labels do not (human, R-7, GLO1)
- Settled: AG6's deferred work — patterns where a manager runs its own sub-team — is captured as a tracked spinoff stub naming what must be proved first: nesting, reporting, restart recovery, cleanup (human, R-7, SPIN1)
- Spinoff: kind deferred | summary: Nested team patterns — a manager role that runs its own sub-team | pain: a manager that cannot recruit its own workers caps every pattern at one level, so work that naturally fans out has to be flattened by hand | why-out: card AG6 defers it until a trial proves nesting, reporting, restart recovery and cleanup; card ACC1 keeps spawning with the contact agent so this feature ships one manifest with one owner | links: relates {this feature's ticket} | status: filed 25 · link-debt
  - Filed 2026-09-17 as https://github.com/midnightblur/afk-driver/issues/25 via `/afk:to-ticket` spinoff mode (human-directed, R-11 SPIN2). The stub names the four things that must be proved before scheduling: nesting, reporting, restart recovery, cleanup. `issue_type` was sent as the label `enhancement` — the github-issues adapter uses `issue_type` directly as a GitHub label, so the Jira word `Enhancement` would have been a label this repository does not have.
  - LINK-DEBT, permanent unless a parent is minted: the intended `relates` target is "this feature's ticket", and this feature has NO tracker ticket — it is tracked on disk under `docs/afk/agent-team-topology/`. There is no key to link issue 25 to, so no link was set and none was invented. If a parent ticket is ever minted for the agent-team-topology feature, link issue 25 to it by hand.
- Settled: every agent is started inside an operating-system job that owns its whole process tree, so closing the job kills the tree whether or not anything was registered. A descendant sweep ships alongside for transports whose start we do not own (human, R-8, CLN1). Repairs what the refuted claim broke: registration alone cannot catch what an agent never reported, and PERM1B enforces nothing.
- Settled: the Codex zero-cost usage read is graded verified by the provider's own documented contract, not by measurement. Nothing in the design depends on it (human, R-8, QTA1)
- Settled: TRN1 handed back, not accepted (human, R-8). The human asks what "desktop tool" meant — it meant 1DevTool, and naming it obliquely on their own page was my error, carried over from the upstream tracker's ban on product names. They state 1DevTool IS installed here, contradicting the trial's "not on PATH". Correct: not on PATH is not the same as not installed, and I relayed the child agent's conclusion without questioning it.
- Verified 2026-09-17, TRN1 follow-up: 1DevTool is installed and licensed (Pro, valid to 2027). The command the design would call, `1devtool-agent`, is NOT on PATH and is not an executable in the install directory. Its own orchestration cache holds exactly 2 runs, both naming that command, and BOTH failed — one `status = error` / exit 2, one `status = timeout` / exit 124 at a 5s timeout; both are almost certainly my own earlier attempts. Its activity log holds 42 tool calls, ALL browser operations — zero spawn, team or stop calls. Its MCP server also failed to connect at this session's start. Verdict: no evidence it can start or stop a team, weak evidence against; not proof of absence, since 2 failures at a 5s timeout is thin and the entrypoint may simply be named something I did not find.
- Settled: both 1DevTool and herdr ship as supported transports. A transport is dropped ONLY when a verified capability check shows it cannot do what the feature needs — never on absence of evidence (human, R-9, TRN2). The human's rule; the check then met it.
- REFUTED 2026-09-17 — my own earlier finding, twice over. (1) `1devtool-agent` EXISTS: `C:\Users\mvu\.1devtool\bin\1devtool-agent-v9.cmd` (also `1devtool-agent.cmd`, `onedevtool.cmd`), in the user profile, NOT on PATH and NOT under `C:\Program Files\1DevTool` — the only two places I looked. It runs `resources\app.asar.unpacked\dist\cli\1devtool-agent.cjs` under `ELECTRON_RUN_AS_NODE`. (2) 1DevTool IS a multiplexer: `%APPDATA%\1devtool\1devtool.json` holds per-project terminal panes with grid ratios and layout; `~\.1devtool\orchestration\control\terminal-links.json` holds a 12-edge directed link graph over 7 terminals with send permissions and hop counts; `hierarchy.json` holds an activated org chart (Orchestrator, 2 Planners, Builder) with `manages` edges and `maxChainDepth: 5`. Lesson recorded: an activity log shows the calls that were made, never the capabilities on offer — I argued absence of evidence as evidence of absence.
- Verified 2026-09-17 (by `--help` read off the installed binary, NOT by a run): `run --to=<target> --prompt-stdin [--terminal|--interactive] [--wait] [--model] [--worktree-path]`; `team start --manifest-stdin` with `{clientRequestId, members:[{role,target,prompt,substrate}]}` plus `team status|list|members|send|ask|stop`; `swarm start --manifest-stdin` with a worker pool and budget; `terminal list --json|status|submit|close`; `stop --team=<id>|--swarm=<id> [--close-terminals|--finish-running]`. 17 agent targets incl. claude, codex, gemini. Attribution rule: run/list/terminal verbs work from anywhere, but resolve/report/link-send need PTY ancestry from a 1DevTool-spawned terminal, and exporting the terminal id by hand does NOT work.
- Verified 2026-09-17: 1DevTool's MCP server exposes NO agent control, deliberately. `tools/list` over stdio returns browser, tasks, request, database and orchestration-draft tools only. Its own source (`mcp-servers\tools\orchestrationDraftTools.js`) states Apply, Activate, Start team, spawn, grant-mint and bind-terminal are human-only with no MCP verb. So the control surface is the CLI, and a detector must never ask the MCP server whether teams are possible.
- Verified 2026-09-17: the live trial is BLOCKED on a human action. 5 `1DevTool.exe` helper processes run, zero main windows — the desktop app is not open. `~\.1devtool\bridges\` is empty, so the MCP bridge has no port to find, which is why the `onedevtool` server failed to connect this session. The shim answers: `fatal: Local terminal attach is disabled or 1DevTool is not running. Enable it in Settings → Terminal → Advance.` Under LED1 the start/stop claim stays UNPROVEN until that trial runs.
- Settled: team agents are started ONE AT A TIME with the tool's `run --to=` command; 1DevTool's native `team start` and `swarm start` manifests are NOT used, and our run manifest stays the only team record (human, R-10, TEAM1). Reason: a pattern is data and may be one-shot, so the shape must live in our pattern description, not in a transport's org chart — otherwise every pattern is written twice, once per transport.
- Settled: on the 1DevTool transport, cleanup closes each terminal by the id recorded in the run manifest, using the tool's own `terminal close`. CLN1's operating-system job covers only agents we start ourselves; the descendant sweep stays the last resort (human, R-10, CLN2). Known residual gap: a background process started INSIDE one of its terminals may survive the terminal's close — the exact shape the Windows trial refuted — and only the sweep catches it, imperfectly.
- Settled: an agent reports and relays only through the channel its own terminal provides. The contact agent, not itself started by the tool, uses only the verbs that work without PTY ancestry — run, terminal list, terminal close, stop — and never the attributed verbs (resolve, report, link send) (human, R-10, ATT1). Constraint imposed by the tool, not a preference.
- Settled: detection looks for the tool's command in the USER PROFILE directory, never on PATH alone, and never asks its MCP server whether teams are possible (that server exposes no control verbs by design). A command that is present but answers that local terminal attach is disabled is reported as a FIXABLE SETTING, naming it — not as a missing tool (human, R-10, DET1). Both halves derive from my own failure in this session; a detector built on either assumption repeats it silently on a user's machine.
- Settled: the PRD proceeds now. The 1DevTool transport is graded SURFACE-VERIFIED AND TRIAL-PENDING — the same grade accepted for the Codex usage-read claim in QTA1: verified by the tool's own contract, not by measurement (human, R-10, LED2). This deliberately bends LED1 once. The live trial is blocked on a human action (open the app, enable Settings → Terminal → Advance); when it runs, a failure edits the transport section rather than redesigning, and does NOT auto-drop the transport, since today's failure looks like a disabled setting rather than a missing capability.
- Exit gate: the requirement tree is EXHAUSTED at 74 cards across 10 rounds. Claim ledger settled — the one refuted row (my own 1DevTool finding) is re-settled against actual behaviour, and the single remaining unproven claim carries an explicit human-accepted grade. Eleven glossary terms written to the plugin `GLOSSARY.md`. Devil's-advocate pass ran and its decision-changing findings became rounds 5 and 8. Access policy grilled. Next: `/afk:to-prd`. Both closing actions approved in R-11.
- Settled: the grill work is committed — the eleven glossary terms as their own commit, the grill documents as a second (human, R-11, CMT1). On the feature branch only; merging stays with the human. `AGENTS.md` is deliberately left UNTRACKED (human, this session): it is an untracked near-copy of `CLAUDE.md` with a find-and-replace that corrupted `.claude-plugin/` into `.Codex-plugin/`; it predates this grill, is not this grill's work, and committing a corrupted harness file is worse than leaving it visible.
- Settled: the AG6 spinoff stub is minted (human, R-11, SPIN2), through `/afk:to-ticket` spinoff mode — the sanctioned tracker writer — never by the grill itself. Dedup checked before minting: the candidate row carried `status: candidate` with no filed key.
- Open: the requirement tree is exhausted at 65 decisions. Exit gate remaining: the four live trials, the glossary entries, the spinoff stub. Then `/afk:to-prd`.
- Open: devil's-advocate pass ran 2026-09-16 over the settled set — 12 findings, none resolved, table in `research/DEVIL.md`. Four attack a settled pair (PERM2 versus PERM1B/C5, PERM2 versus PAT1, H2 versus BRIEF goal 5, L1 versus H3) and three bear on cards open in round 4 (ACC2 versus H1, LIM1 versus H2, LIM3's unwritable mark). Round 5 is built from what survives.
- Superseded: round 4 not yet built — remaining are the access-policy grill and the devil's-advocate pass over every settled item. Never write the page file while a round is open for answering: lavish's watcher reloads the page on any write and destroys unsent answers (verified in lavish-axi 0.1.43; issue drafted, fingerprint 8a43a4d2f8a5).
- Superseded: round 3 not yet built — remaining topics are the built-in pattern list, each role's permissions, environment detection with the optional herdr install in setup, then the access-policy grill and the devil's-advocate pass.
- Closed: AG1, AG2 and AG3, reopened by the human (R-1), settled in R-2 by H2, H3 and S3. The provider check may be unnecessary: a usage limit resets on its own, and the limited model cannot run the check that would detect it; provider downtime is the other case and looks deterministically checkable. For a single-model role such as builder or orchestrator, continuing in an equivalent model from another provider is acceptable; the dying model cannot hand off its own work, so a handoff mechanism is needed. Where no fallback exists, or the role cannot fall back (multi-model planners in debate), wait, or let the human continue without it.
- Open: agent-to-agent talk rules (R-1, T4 steer) — who may talk to whom. Manager-to-worker and worker-to-manager are obvious; siblings and same-role multi-model agents are not. Weigh token cost, since each bridge costs, against honesty, transparency and control, since agents can agree to report a false consensus.
- Open: pattern guidance (R-1, T5 steer) — agents help the human pick a built-in pattern, or propose a custom one for the occasion.
- Open: Round 1 answered in full; round 2 not yet built or rendered. Do not re-render while the human has the page open — the reload is theirs to trigger.
- Superseded: Round 1 in play, updated with the converge result (research/VERDICT-claude.md, research/VERDICT-codex.md: 32 of 33 points AGREE) — terms T1–T7, switches D1 and C1–C5, debater agreements AG1–AG7 to audit, human values L1 (wait horizon), L2 (weekly limit), L3 (OS scheduler), K1 (commit the store). Page: grill-requirements.round.json → grill-requirements.html
  steer: all agents should run on auto mode (human, this session — C5 asks whether it becomes the product default)
  steer: "the design should make sure the planners debate, and grill is only when they decide to agree and disagree and no further discussion needed and users would be needed" (human, R-1 — converge round 3 runs per research/CONVERGE.md before disputed items reach the human; only its DISAGREE-FINAL rows enter round 2)
  steer: "usage limit might hit, ideally users won't have to babysit and come back to continue the work. if the agents can know if a provider is limited and when it's reset, then schedule to return, that would be great" (human, R-1 — dispute 8 in research/CONVERGE.md; facts in research/FACTS-quota.md)
