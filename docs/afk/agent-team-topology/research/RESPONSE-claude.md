# Response — side claude, converge round 3

Author: debater-fable, 2026-09-15. Answers every objection in `CRITIQUE-codex.md` §A, then states a final position per open dispute in `CONVERGE.md`. Fact ids keep their source namespace (`FACTS-claude.md`: PL/SP/KS/HR/DT/CN/CX/EV; `FACTS-codex.md`: Cnnn). Human rulings in `../GRILL-LOG.md` are not re-argued. Re-checks run this round are in §3; a row citing `R-n` rests on that run.

## 1. Objections (`CRITIQUE-codex.md` §A)

| obj | verdict | reason + fact ids | resulting position |
|---|---|---|---|
| 1 | accept | "Nothing to kill" held only after confirmed exit. A bounded run showed 23 live processes before timeout cleanup (C042, C044); Windows descendant cleanup is not established (C041). `claude rm` deletes the session and its worktree; `claude stop` keeps the conversation (R-1; CN-05). | Every `turn` run gets a supervisor holding pid + creation time + timeout (PL-33 shape). `close` returns `{exited, cleanup: ok\|partial[pids], session_id}`; `keep_session: true` maps to `stop`, never `rm`. A `closed` roster event requires `exited: true`. |
| 2 | accept | An owned worktree bounds git state, not machine access (DT-12, C046, C079). Claude `--permission-mode default` is not demonstrated read-only enforcement (CN-03). The human steered every agent to auto mode (`GRILL-LOG.md` steer, C5 pending) — that steer sets the default, not the mechanism. | Each role declares a required permission capability (`read-only` / `write-in-cwd` / `unrestricted`); each transport kind advertises what it enforces; a mismatch refuses spawn with `transport_cannot_enforce`. `unrestricted` is legal only when config names the role and the cwd is a plugin-created worktree off a non-protected branch. Whether the product default is `unrestricted` is human switch C5. |
| 3 | accept | Hash, exit code, and fact-reference checks establish evidence properties, not the conclusion (KS-06, C078). The plugin's own coverage ledger already keeps claim `kind` apart from node identity and folds conservatively (worst kind wins, `LEDGER-FORMAT.md:289`, R-4). | Two fields per row: `evidence_status` (`fresh` / `stale` / `gone`, deterministic) and `verdict` (`verified` / `inferred` / `unverified` / `refuted`, set only by a producer with an executable assertion or by a frontier reviewer). The deterministic pass emits `stale` / `gone` events only; it never raises a verdict. Dispute 6 rests on this row. |
| 4 | accept | A producer label cannot prove a command is read-only or unchanged since capture (C068, C076); replay is a new authority the plugin has not granted anywhere (PL-20). | Automatic re-run is limited to a plugin-owned probe registry (fixed executable, validated arguments, timeout, output redaction). Any other `command` evidence is kept as excerpt + exit code and re-verified only by a producer or reviewer, never by the script. |
| 5 | accept | `sha1(claim)` merges same sentences from different scopes (KS-05 is investigation-local, not repo-wide); last-event-wins lets a stale verification overwrite a supported refutation. The coverage ledger refuses conflicting rows rather than picking one (`LEDGER-FORMAT.md:287-291`, R-4). | `id = sha1(repo, spec-dir, subject, claim)[:8]`. Events split into `observation` (evidence checks) and `adjudication` (verdict changes); the latest adjudication wins, an observation never overrides one. Two adjudications disagreeing without a `supersedes` link set `disputed`. The writer script holds a file lock and takes an idempotency key, so a retried append is a no-op. |
| 6 | accept | Ephemeral Codex runs persist no session (C047, C067, C080); resumption was not reproduced this round (CN-04, HR-14 unreproduced). Closing after a failed verify with no receipt loses findings silently. Evidence citing `plan/` paths breaks at gc (KS-17, R-3). | `resume` is a per-kind, per-provider capability field; a role may set `needs_history` only where it is `verified`. Retirement requires a receipt id returned by the writer script and carried on the `closed` event. Forced termination writes a `lost` event naming unfinished briefs. Every `path` evidence row stores excerpt + line hash so the fact outlives the file; after gc `evidence_status` becomes `gone`, the verdict stays. |
| 7 | accept | `solo` = `native` still spawns subagents; the brief's goal 6 names `none (single agent)` and the human holds D1 open (`GRILL-LOG.md`). `second-opinion` requiring another provider contradicts goal 4 (team with one provider). `providers.cross_model: true` without `agent:` was self-contradictory (SP-21, PL-16). | Add value `none` (semantics in dispute 2). `second-opinion` gets `cross_provider: preferred` by default; `required` is an explicit pattern setting. Combination matrix: `agent:` gates team transports only; `providers.cross_model: true` alone loads the `headless` kind for single-role turns because a CLI is its only dependency; `team:` without `providers:` resolves every `provider: auto` to the harness; unset everything = today (PL-16 byte-identical gate test kept). |
| 8 | accept | The Codex "Reply OK" turn cost 18,541 tokens and 19.6 s (CX-04); the Claude one 9.1 s (CN-13). Startup context and hooks are not bounded by a trivial prompt (C042 shows hooks firing before any answer). Login can be read without generation on both providers (R-2: `codex login status` exit 0; `claude auth status --json` exists, CN-12, C069). Quota is unmeasured either way (C077). | Default probe spends no generation: binary → login → account/model metadata where a provider exposes it (C071, `inferred` until one live call, C072). Quota stays `unknown`. A generation rung exists as an explicitly budgeted, separately configured check (see dispute 1). |
| 9 | accept | A human-named provider is the human's choice; substituting it is not a two-way door the agent may open (`DECISIONS.md` test as cited in KS-01). 1DevTool's own routing rule refuses silent reroute of an assigned agent (R-5). Explicit fallback lists exist as a mechanism (C031 receipts, C034 fallback controls). | A named provider that fails parks the step with `provider_unavailable(reason)`; autopilot's status-agnostic park handles it hands-off. Fallback runs only from `provider: auto` or an explicit `fallback: [..]` list on the role. Transport degradation likewise keeps the role's declared permission, independence, and messaging needs or refuses. |
| 10 | accept | herdr's wait tracks lifecycle state, not a turn: "if the agent is already working, completion of the active turn may satisfy it" (R-6; HR-07, HR-09). A file written by a helper wakes nobody (CN-15). | `send` returns `{submission_id}`; each brief names a `result_path` that must contain that id; `wait` completes on a matching result file, `blocked`, or timeout, never on state alone. Wake mechanism: the manager is inside a blocking `wait` call with a timeout; the watchdog's exit code (PL-33) is the only post-timeout signal. No idle-agent trigger is promised. |
| 11 | accept (advisory) | Terminal tools carry attributed messaging, consent, approval interaction, and controller state (DT-08, DT-11, C029–C033, C051). | Capability fields per kind: `messaging_attributed`, `approval_ui`, `controller_state`, `visibility`. Content stays in files; approvals and attributed messages never do. |
| 12 | accept (advisory) | Contact synthesis and single-writer stamps are never delegated (PL-32); every review round runs the frontier tier (PL-29). The proposal's "tier 1 sol vs opus" weakened round one. | Drop model cascading by tier; cascade by open points only, frontier at every round. Planners draft from supplied artifacts, never synthesize the conversation. Referee = the orchestrating session; when it shares a model with a side, the round file records `referee_model_shared_with: <side>` and a third usable provider, if any, referees instead. |
| 13 | accept | Phase 1 bundled six mechanisms. The value to prove first is "a fresh consumer works from a closed planner's evidence" — this converge round is that test in Markdown form (`CONVERGE.md` "deliberate test"). A committed rendered `FACTS.md` is a second source of truth (KS-10 keeps the lesson ledger single-form). | Phase 1 per dispute 4. Rendered digests are produced on demand into the spawn prompt or a temp path, never committed. |

## 2. Open disputes — final positions

| # | dispute | position | rests on |
|---|---|---|---|
| 1 | Availability probe: real generation call? | Default: no. Rungs 0–1 (binary, login) every session start; rung 2 metadata where documented. A generation rung is a distinct config value `providers.probe: live`, with a per-provider token/time budget and the cheapest tier model, run once before a hands-off run when set, because no human is present when the first cross-provider call fails (SP-07, `CRITIQUE-claude.md` a.1). Interactive default `auth`. A rung-2 pass never asserts frontier-model access (C078). | CN-12, CN-13, CX-03, CX-04, C069–C078, R-2 |
| 2 | Topology values: strict single-agent value? | Yes, `none`, explicit opt-in, distinct from unset. Semantics: no spawn of any kind; `/afk:autopilot` refuses `none` because its doctrine depends on fresh per-subtask agents (SP-01); interactive skills run inline and stamp `independence: none` on every review and adversary report so `/afk:preflight` and the human see it. Whether the value ships is human D1 (`GRILL-LOG.md` round 1); this is our recommendation to it. | SP-01, SP-09, SP-12, PL-16, BRIEF goal 6 |
| 3 | Named provider fails: fall back or stop? | Stop (park). Fallback only from `auto` or an explicit fallback list. Same rule for transport kinds. | KS-01, C031, C034, PL-20, R-5 |
| 4 | Phase 1 scope | Knowledge store only, around today's native children: writer script (`add` / `verify` / `digest` / `render`), `{spec-dir}/knowledge/FACTS.jsonl`, the repo catalog (dispute 5), receipt-before-close added to the existing spawn brief templates (SP-04), the byte-identical-defaults gate test (PL-10, PL-16). Acceptance: close a planner; a fresh agent answers a question from its rows without repeating the research. Provider probe, `headless` kind, `second-opinion`, herdr, 1DevTool follow in that order; herdr before 1DevTool because dogfooding runs in herdr today (HR-25). | KS-05, KS-10, KS-16, KS-17, C023, C024, PL-39 |
| 5 | Knowledge store: spec folder only or also a catalog? Survives gc? | Both. Canonical rows live in `{spec-dir}/knowledge/FACTS.jsonl`; a repo-level catalog holds pointers only (id, subject, spec-dir, verdict, evidence_status), regenerated by the writer script, never hand-edited. Survives `/afk:gc` without a gc edit: gc deletes a fixed list and keeps "any file not on the delete list" (R-3). Evidence inside `plan/` is gone after gc; rows keep excerpt + hash, `evidence_status: gone`; git history holds the bytes. Commit or gitignore is human question 11 of `PROPOSAL-claude.md` §i. | KS-08, KS-17, R-3, C024 |
| 6 | "Verified" for a stored fact | Two axes, never one word. `evidence_status` answers "does the evidence still check" (deterministic). `verdict` answers "is the conclusion confirmed" (producer's executable assertion or frontier review). A row is usable without re-derivation only when `verdict: verified` and `evidence_status: fresh`; any other pair is cited as inferred by the consumer. The two FACTS ledgers this round used "verified" for different bars (claude: re-checkable now; codex: within the stated evidence boundary), which is the defect this split removes. | KS-05, KS-06, KS-09, C077, C078, `LEDGER-FORMAT.md:289` (R-4) |
| 7 | `hierarchy` pattern: now or deferred? | Deferred. No transport has proved nested spawn plus report-up: DELEGATION caps nesting at 3 (PL-30), Claude teams forbid nested teams (CN-02), 1DevTool swarms forbid nesting (C019). 1DevTool's hierarchy is a user-configured seat mechanism with `report` and `whoami` (R-7; DT-08, C032, C033) — a later `1devtool` kind can bind a `hierarchy` pattern to seats once one live team stop is proved (C045 gap). | PL-30, CN-02, C019, C032, C033, C045, R-7 |

## 3. Re-checks run this round (read-only; no agent started)

| id | command | result |
|---|---|---|
| R-1 | `claude --help` (rm / stop rows) | `rm <id>`: "Delete a background session, and its worktree when that is safe". `stop\|kill <id>`: "Stop a background session. Its conversation is kept: `claude attach <id>` opens it again". `--bare` exists: "skip hooks, LSP, plugin". Version 2.1.272. |
| R-2 | `codex login status`; `claude auth status --help` | Codex: "Logged in using ChatGPT", exit 0, no generation. Claude: `--json` output flag present (default). `codex doctor` present in `codex --help`. |
| R-3 | `skills/afk/gc/SKILL.md:47-63` | Delete list: `plan/`, `GRILL-LOG.md`, `TICKET.md`, `*.adf.json`. Keep: named evergreen files "and any file not on the delete list". |
| R-4 | `skills/utils/investigate/LEDGER-FORMAT.md:114,287-291` | `line_hash` identity; fold refuses conflicting rows; claim fold: "the worst `kind` wins (`unverified` > `inference` > `fact`)". |
| R-5 | `~/.claude/skills/1devtool-orchestrator/SKILL.md:328` | "do not reclaim that part, use your own tools, or silently reroute it … Stop and ask the user how to proceed unless the user already explicitly authorized fallback for that part." |
| R-6 | `herdr --skill` (prompt/wait section) | "This wait tracks lifecycle state, not an individual turn; if the agent is already working, completion of the active turn may satisfy it." "A timeout or stalled response does not prove the prompt was never delivered." |
| R-7 | `~/.claude/skills/1devtool-orchestrator/SKILL.md:330-355` | Hierarchy = ranked seats over terminal links; `report --prompt-stdin` to one manager, `--blocked` escalation, `whoami` prints seat/tier/manager/subordinates; enforced by 1DevTool. |

Not re-run: any generation call (CN-13, CX-04 stand as attributed measurements), herdr pane lifecycle (HR-01–HR-25), 1DevTool team stop (C037, C045 gap stays open).
