# Facts — 1DevTool and Codex transport

Observed: 2026-09-15. Owner: `planner-codex`. Read `LANGUAGE.md` before extending this ledger.
CLI means command-line interface. MCP means Model Context Protocol. PID means process identifier.
`verified` applies to the stated evidence boundary: documentation, source, or live behavior.
Documentation alone does not establish live availability.

## Evidence locations

| key | location |
|---|---|
| CLI | `C:\Users\mvu\.1devtool\bin\1devtool-agent-v9.cmd` |
| SRC | `C:\Program Files\1DevTool\resources\app.asar.unpacked\dist\cli\1devtool-agent.cjs` |
| ORCH | `C:\Users\mvu\.codex\skills\1devtool-orchestrator\SKILL.md`, managed version 35 |
| TASK | `C:\Users\mvu\.codex\skills\1devtool-tasks\SKILL.md` |
| MCP | [Captured tool declarations](EVIDENCE-codex-mcp.json) |
| LIVE | [Live process evidence](EVIDENCE-codex-live.json), generated by [probe script](PROBE-codex.ps1) |
| FLAGS | [Rejected duplicate flag](EVIDENCE-codex-flag-error.json) |
| CP | [Provider evidence](EVIDENCE-codex-providers.md), stable row identifiers `CP01`–`CP36` |

## 1DevTool: detection and authority

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C001 | The installed shim runs Electron as Node against `SRC`. | `Get-Content CLI`: `ELECTRON_RUN_AS_NODE=1`, then `1DevTool.exe ...1devtool-agent.cjs`. | verified | Installation changes |
| C002 | This planner runs in herdr, outside an attributed 1DevTool session. | Environment: `HERDR_ENV=1`, `HERDR_PANE_ID=w5:p3`; `CLI whoami` → `session:false`, exit 0. | verified | Session changes |
| C003 | `whoami` is a cheap session probe; absence of its identity variable returns `session:false`. | SRC:6872–6886; live C002. | verified | CLI changes |
| C004 | 1DevTool identity includes terminal, headless credential, worker, and run variables. | SRC:170–190: `ONEDEVTOOL_TERMINAL_ID`, `ONEDEVTOOL_HEADLESS_CREDENTIAL`, `ONEDEVTOOL_WORKER_ID`, `ONEDEVTOOL_RUN_ID`. Credential values were not read. | verified | Identity protocol changes |
| C005 | A manually exported terminal identifier does not grant attributed access. | `CLI --help`, Attribution; SRC:1427–1441: app checks ancestry against a terminal it spawned. | verified | Authorization implementation changes |
| C006 | Team capabilities are unavailable from this external process tree. | `CLI team capabilities` → `No compatible 1DevTool instance owns the calling terminal`, exit 1. | verified | Caller or controller changes |
| C007 | Installation detection does not prove provider usability. | `CLI list --json`: Codex and Claude `status:"unverified"`, executable paths present. | verified | CLI or machine changes |
| C008 | The terminal CLI offers list, view/attach, submit, status, and close. | `CLI --help`, terminal commands: status `idle/busy/unknown/closed`; submit returns `submissionId`. | verified | CLI changes |
| C009 | External terminal control requires the app's local-terminal-CLI setting. | C006 error recommends enabling Settings → Terminal → `Enable local onedevtool terminal CLI`. | verified | App settings or implementation changes |

## 1DevTool: spawning and collecting

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C010 | `run` accepts stdin or a permitted prompt file; `--prompt=<text>` is unsupported. | `CLI --help`: `--prompt-stdin`, `--prompt-file`; files restricted to cwd or temporary directory, without symlinks. | verified | CLI changes |
| C011 | Direct headless `run` works from this external terminal. | LIVE: Codex banner, session identifier, model, and hooks appear before timeout. | verified | CLI, provider, or environment changes |
| C012 | Direct `run` selects target, model, cwd, category, timeout, and extra target flags. | `CLI --help`, Run options. `--no-link` requests fresh headless execution when a link exists. | verified | CLI changes |
| C013 | A terminal run creates a one-member team and returns `runId` and `teamId`. | SRC:6299–6338; `CLI --help`, `--terminal`. This path was not live-tested. | verified | Controller or CLI changes |
| C014 | Terminal write categories can create a dedicated worktree. | `CLI --help`: `implement/test/debug`; `--shared-cwd` selects shared checkout; `--worktree-path` selects an existing dedicated worktree. | verified | Route policy changes |
| C015 | `team start` accepts a declarative manifest with stable member identities. | ORCH, “How to call it”: `clientRequestId`, members with role/target/prompt/substrate; response `teamId/memberId/runId`. SRC:6439–6447. | verified | Team schema changes |
| C016 | Team members can receive different prompts and providers. | ORCH, “Agent Team vs Swarm”; manifest examples. | verified | Team schema changes |
| C017 | `swarm start` sends one `brief` to bounded isolated workers. | ORCH, “How to call it”: `count`, `targets`, `poolSize`, `budget`, `substrate`, `sandbox`; exact task key `brief`. | verified | Swarm schema changes |
| C018 | Documented headless swarm targets with enforced sandboxing are Codex, Claude, and Cursor. | ORCH, paragraph “Headless Swarm workers”; unsupported targets are refused. Not a direct-run guarantee. | verified | Provider support changes |
| C019 | Swarm workers do not message one another or start nested orchestrations. | ORCH, “Agent Team vs Swarm”. | verified | Swarm policy changes |
| C020 | Team and swarm can use headless or terminal execution; pipeline requires live terminals. | ORCH, “Choosing a pattern” and “Choosing headless vs a real terminal”. | verified | Controller capabilities change |
| C021 | `collect --run` returns a correlated result; still-running responses are normal. | SRC:7403–7432; ORCH, “How to call it”. | verified | Result protocol changes |
| C022 | Swarm collection supports cursor and limit; default limit is 20. | `CLI --help`, `collect --swarm`; SRC:7403–7432. | verified | Result protocol changes |
| C023 | Headless output is bounded and can be truncated. | SRC:700–724,790–795; ORCH states approximately 100,000 characters. | verified | Output cap changes |
| C024 | Direct runs persist run metadata, prompt, output, errors, and truncation state locally. | SRC:6240–6253,6396–6414. | verified | Storage implementation changes |

## 1DevTool: messages and supervision

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C025 | Follow-up submissions require explicit team/member identifiers and a submission identifier. | `CLI send --team=<id> --member=<id> --submission-id=<uuid> --prompt-stdin`; ORCH states retry deduplication. | verified | Submission protocol changes |
| C026 | Peer messaging includes team send, ask, reply, and paged messages. | SRC:6787–6869; `team ask` waits up to 600 seconds; message pages allow 1–100 rows. | verified | Messaging protocol changes |
| C027 | Team status/list/members/connections and swarm status/pause/resume support supervision. | `CLI --help`; SRC:6591–6785. | verified | CLI changes |
| C028 | Team recovery uses expected-run and client-request identifiers. | SRC:6539–6567: `team recover --team --member --expected-run --client-request-id`, optional target/model. | verified | Recovery protocol changes |
| C029 | Links provide send/request/status, read/screen/peek/notes, and publish. | `CLI --help`, link commands; ORCH, “Existing terminal links” and “Permission flags”. | verified | Link protocol changes |
| C030 | Peer reads require explicit consent and correct session/worktree scope; team membership alone grants no read. | ORCH, “Permission flags”; member aliases resolve to the same authorized link. | verified | Consent policy changes |
| C031 | Link receipts distinguish acknowledged delivery, uncertain delivery, and pending confirmation. | ORCH, “Existing terminal links”: `delivered`, `delivery-unconfirmed`, exit 3 `queued (confirmation-required)`. Uncertain delivery is not automatically retried. | verified | Delivery protocol changes |
| C032 | A hierarchy routes tasks to subordinates and reports to one manager. | ORCH, “Agent hierarchy”; `whoami` includes seat/tier/manager/subordinates; role card controls routing. | verified | Hierarchy or seat changes |
| C033 | `report` supports blocked escalation, continuation, and pipeline completion. | SRC:6888–6925; `CLI --help`: `report/handoff`, `--blocked`, `--continue`, `--complete`. | verified | Routing protocol changes |
| C034 | Recovery controls can confirm submission, resolve completion, or apply a fallback action. | `CLI --help`: `confirm-submit`, `resolve done/error/cancelled`, `fallback retry/headless/reassign/skip/close`. | verified | Recovery protocol changes |
| C035 | Workspace roster/send/broadcast/collect/operation support coordination across projects. | `CLI --help`; ORCH, “Workspace Control”: membership permits roster; delivery still requires a link. | verified | Workspace or consent policy changes |
| C036 | Link broadcast/vote/decisions provide a bounded vote interface. | `CLI --help`: vote question, options, quorum; individual `link vote`, `link decisions --open`. | verified | CLI changes |

## 1DevTool: stopping and live proof

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C037 | `stop` targets exactly one team or swarm; `--close-terminals` requests terminal cleanup. | SRC:7434–7461; `CLI --help`. | verified | Stop protocol changes |
| C038 | `--finish-running` is swarm-only and conflicts with `--close-terminals`. | SRC:7434–7461. | verified | Stop protocol changes |
| C039 | Terminal close kills its process, but refuses seats belonging to live orchestration. | `CLI --help`, `terminal close`: use team stop for those seats. Documented; not live-tested here. | verified | Controller changes |
| C040 | Direct headless timeout is clamped to 5–600 seconds. | SRC:635–638,667–669,6234–6239. | verified | CLI changes |
| C041 | Timeout source sends termination, then forced termination after 2 seconds; timeout returns 124. | SRC:725–775. General descendant cleanup on Windows is not established by this source alone. | verified | Process implementation changes |
| C042 | One bounded live run started Codex and was terminated by the 5-second timeout. | [PROBE-codex.ps1](PROBE-codex.ps1) → [EVIDENCE-codex-live.json](EVIDENCE-codex-live.json): root PID `20576`; `exitCode:124`, `timedOut:true`, reported duration 5 seconds, measured duration 6.36 seconds. | verified | Executable or environment changes |
| C043 | The live Codex session identifier was `01a0a5f9-05bb-76d0-8882-e8fbf57dcf61`. | LIVE stdout: Codex 0.154.0 banner, model `gpt-5.6-sol`, session identifier. | verified | Historical evidence is replaced |
| C044 | All 23 observed processes had exited; cleanup killed no unrelated process. | LIVE `processes`: every `exited:true`, every `requiredCleanup:false`; later `Get-Process` over those identifiers returns no rows. | verified | Historical evidence is replaced |
| C045 | The trial proves start and timeout termination; it does not prove team stop or successful inference. | LIVE has startup hooks and no assistant result; C006 denies attributed team controls. | verified | A later bounded controller trial supplies evidence |
| C046 | The live direct-run path disables Codex approvals and sandboxing. | LIVE banner: `approval: never`, `sandbox: danger-full-access`. ORCH says the CLI injects provider bypass flags. | verified | Launcher policy changes |
| C047 | 1DevTool already supplies Codex `--ephemeral`. Adding it again fails argument parsing. | [EVIDENCE-codex-flag-error.json](EVIDENCE-codex-flag-error.json): `the argument '--ephemeral' cannot be used multiple times`, exit 2. Corrected run removes the extra flag. | verified | Launcher command changes |
| C048 | The initial rejected launch left no observed process alive. | FLAGS: root PID `42012`, `exited:true`; parser fails before an inference turn. | verified | Historical evidence is replaced |

## MCP orchestration and Tasks

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C049 | This session exposes 4 `orchestration_*` tools. | [EVIDENCE-codex-mcp.json](EVIDENCE-codex-mcp.json) lists `recommend`, `explain`, `propose_chart`, `chart_op`. Enumeration is scoped to this session. | verified | Enabled tools change |
| C050 | Recommend and explain are read-only; recommend returns hierarchy/pipeline/router/ephemeral-team/none. | MCP declarations for `orchestration_recommend` and `orchestration_explain`. | verified | Tool schema changes |
| C051 | Chart writes require a human-enabled draft and matching revision/hash. They do not start teams. | MCP declarations: `propose_chart`, `chart_op`, `basePolicyHash`, `baseRevision`; neither applies or binds live terminals. | verified | Tool or policy changes |
| C052 | The exposed orchestration tools are not operational in this session. | Live `orchestration_explain({})` → HTTP 403, `Feature 'orchestrator' is not enabled`. | verified | Feature enablement changes |
| C053 | Tasks provides next/list/get, create/update/link/comment, complete, request_approval, and wait. | MCP enumerates 10 `tasks_*` declarations. | verified | Enabled tools change |
| C054 | Task creation leaves work unassigned; updates cannot choose an assignee. | MCP `tasks_create/tasks_update`; TASK:35–41,55–69. Human dispatch owns assignment. | verified | Task policy changes |
| C055 | Task links add dependencies, parent, and related tasks; cycles are rejected. | MCP `tasks_link`. | verified | Task graph rules change |
| C056 | Task approval returns a gate identifier; wait reports open/resolved/stale/timeout. | MCP `tasks_request_approval/tasks_wait`; TASK:72–84. Timeout is not approval. | verified | Gate protocol changes |
| C057 | Completion can close a task or open a review gate. | MCP `tasks_complete`; summary addresses acceptance criteria. | verified | Task policy changes |
| C058 | Tasks requires attributed assignment; an empty next result does not authorize other work. | MCP `tasks_next`; TASK:35–41. | verified | Task policy changes |
| C059 | Direct prompts can proceed when Tasks is absent or unattributed. | TASK:20–31. | verified | Skill changes |
| C060 | None of the exposed MCP orchestration or Tasks declarations provides live spawn/kill. | Exhaustive C049/C053 enumeration; chart operations explicitly exclude starting teams. CLI owns the investigated execution interface. | verified | Enabled tools change |

## Codex transport and provider availability

| id | claim | evidence (command + output excerpt, or path:line) | status | expires-when |
|---|---|---|---|---|
| C061 | Installed Codex is 0.154.0; this shell does not resolve it through PATH. | [EVIDENCE-codex-providers.md](EVIDENCE-codex-providers.md), CP01–CP02; binary `C:\Users\mvu\AppData\Local\Programs\OpenAI\Codex\bin\codex.exe`. | verified | Installation or environment changes |
| C062 | `codex exec` accepts stdin and explicit model/configuration choices. | CP03–CP05: `-m`, `-c`, optional local provider. | verified | CLI changes |
| C063 | Codex supports read-only/workspace-write/full-access sandbox settings and separate approval policy. | CP06–CP10: top-level `-a never/on-request`; explicit sandbox; `--approve-for-me`; bypass disables both safeguards. | verified | CLI changes |
| C064 | Working directory, extra writable roots, and managed worktrees are configurable. | CP11: `-C`, `--add-dir`, `--worktree`. | verified | CLI changes |
| C065 | Codex exposes event output, diagnostics, final-answer file, and structured-answer schema. | CP12–CP14: `--json`, stderr, `-o`, `--output-schema`. JSON means JavaScript Object Notation. | verified | Event or CLI schema changes |
| C066 | Persisted Codex sessions can resume by explicit identifier and change model. | CP16–CP18: `exec resume`, `--model`, `--json`; `exec fork` exists. Live resume was not tested. | verified | Session schema changes |
| C067 | Ephemeral runs suppress persisted sessions; concurrent workers should avoid ambiguous `--last`. | CP15–CP16; inference: prefer explicit session identifier for concurrent resumption. | inferred | Session selection rules change |
| C068 | Local sandbox failure can conceal valid Codex authentication. | CP19–CP20: sandbox `Could not find home directory`; approved outside check → `Logged in using ChatGPT`, exit 0. | verified | Sandbox or account changes |
| C069 | Claude 2.1.272 reports a first-party Max login. | CP21–CP22: selected `auth status --json` fields `loggedIn:true`, `authMethod:claude.ai`, `subscriptionType:max`. | verified | Login, subscription, or CLI changes |
| C070 | Login status alone does not establish remaining quota or next-request acceptance. | CP23; neither status result contains a current quota measurement. | inferred | A scoped live provider check supplies evidence |
| C071 | Codex documents non-generation methods for account, quota, and model discovery. | CP24–CP27: `account/read`, `account/rateLimits/read`, `model/list`; [App Server](https://learn.chatgpt.com/docs/app-server). | verified | Application-server protocol changes |
| C072 | Those Codex methods can be requested without starting a generation turn. | CP27, inferred from independently documented request methods; initialize protocol, omit `turn/start`. | inferred | Protocol changes |
| C073 | Claude documents `/usage` for limits and reset times. | CP29; [Errors](https://code.claude.com/docs/en/errors). Non-interactive invocation was not established. | verified | Claude interface changes |
| C074 | Claude status-line quota fields require a preceding provider response. | CP30–CP31; [Status line](https://code.claude.com/docs/en/statusline). Reuse existing data; absence is unknown. | verified | Status schema changes |
| C075 | A Claude interactive status session is not guaranteed to be free of background token use. | CP32; [Costs](https://code.claude.com/docs/en/costs). | verified | Billing or CLI behavior changes |
| C076 | Provider configuration can select billing credentials different from the stored subscription. | CP33; [Authentication](https://code.claude.com/docs/en/team). | verified | Credential precedence changes |
| C077 | Neither provider's actual remaining quota was measured. | CP35; the bounded launch reaches startup hooks but produces no assistant result. | unverified | Fresh scoped quota evidence arrives |
| C078 | A zero-cost probe cannot guarantee the next selected model request will succeed. | CP34–CP36; inference includes changing quota, model entitlement, network, and service capacity. | inferred | Provider offers a stronger guarantee |
| C079 | Direct headless Codex defaults include bypass, ephemeral sessions, and skipping the repository check. | SRC:31–34: `--dangerously-bypass-approvals-and-sandbox`, `--ephemeral`, `--skip-git-repo-check`; SRC:139–151 builds `exec ... -`. | verified | Launcher command changes |
| C080 | The inspected direct-run wrapper does not provide durable Codex continuation. | Inference: C079 forces ephemeral mode; scoped SRC search finds no Codex headless resume handling. Native Codex resume is separate, C066. | inferred | Wrapper gains continuation |

## Trial limits and cleanup

The authorized trial uses one trivial prompt and no requested tool actions.
One earlier invocation fails argument validation; the corrected invocation reaches Codex startup.
The timeout supplies live termination evidence. No team or terminal tab is created by this headless path.
No team identifier is returned. LIVE retains the Codex session identifier and every observed PID.
Polling cannot prove that every short-lived descendant was observed. It proves cleanup for the recorded process set.
1DevTool keeps its own run history under C024; research retains that evidence and does not delete shared history.
Live terminal spawn, team stop, swarm cancellation, peer delivery, and provider inference remain untested.

## Ledger consumption

Use stable row identifiers in proposals. Recheck a row when its expiry condition holds.
Treat source and documentation claims as capability evidence, not operational proof.
Preserve a failed check with its environment and error; never rewrite it as a provider-authentication failure.
Design consumer: [PROPOSAL-codex.md](PROPOSAL-codex.md).
Contact review: compare that proposal with [PROPOSAL-claude.md](PROPOSAL-claude.md) before settling the shared design.
Round 1 completes before this planner reads the other proposal. Round 2 records the authorized comparison in `CRITIQUE-codex.md`.
