# Verdict — side claude, converge round 3, Step 2

Author: debater-fable, 2026-09-15. Files this side wrote: `RESPONSE-claude.md` (Step 1), `VERDICT-claude.md` (this file). Compared against `RESPONSE-codex.md`. Fact ids: `FACTS-claude.md` (PL/SP/KS/HR/DT/CN/CX/EV), `FACTS-codex.md` (Cnnn), `FACTS-quota.md` (QH/QC/QW/QR), `RESPONSE-claude.md` §3 (R-n). Human rulings in `../GRILL-LOG.md` stay binding.

## 1. Objections in `CRITIQUE-codex.md` §A (raised against side claude; answered in `RESPONSE-claude.md` §1)

| id | verdict | agreed position | reason |
|---|---|---|---|
| A.1 | AGREE | Supervisor per `turn` run (pid, creation time, timeout); `close` returns `{exited, cleanup, session_id}`; `keep_session` maps to `stop`, never `rm`; a `closed` event requires `exited: true`. | Side claude accepted; `RESPONSE-codex.md` a.12 restates the `stop`/`rm` split (CN-05, R-1) and separate exit/cleanup receipts (C041, C044). |
| A.2 | AGREE | Roles declare a required permission capability; kinds advertise what they enforce; mismatch refuses spawn. `unrestricted` only for a named role in a plugin-created worktree off a non-protected branch. Product default = human switch C5. | Both sides: a worktree bounds git state, not the machine (DT-12, C046, C079); `RESPONSE-codex.md` a.7 adds "test the selected permission path without assuming bypass" (HR-22 source-session scope), which side claude adopts. |
| A.3 | AGREE | Two fields per fact: `evidence_status` (deterministic) and `verdict` (producer assertion or frontier review). Deterministic passes only downgrade. | Identical to `RESPONSE-codex.md` dispute 6 (KS-05–KS-07, C078; `LEDGER-FORMAT.md:289`, R-4). |
| A.4 | AGREE | Automatic replay limited to a plugin-owned probe registry; other command evidence is excerpt + exit code, re-verified only by a producer or reviewer. | Side claude accepted (C068, C076, PL-20); no counter-position in `RESPONSE-codex.md`. |
| A.5 | AGREE | `id = sha1(repo, spec-dir, subject, claim)[:8]`; observation vs adjudication events; latest adjudication wins; unresolved conflict → `disputed`; file lock + idempotency key in the writer. | Side claude accepted; `RESPONSE-codex.md` a.3 names the same properties: scoped, serialized, idempotent, explicit conflict handling (KS-05, KS-10). |
| A.6 | AGREE | `resume` is a per-kind, per-provider capability; retirement needs a receipt id; forced termination writes a `lost` event; evidence rows keep excerpt + hash. | Side claude accepted (C047, C067, C080, KS-17, R-3); `RESPONSE-codex.md` dispute 4 requires "interrupted capture and visibly missing knowledge" in phase 1 — same mechanism. |
| A.7 | AGREE | Add `none`; `second-opinion` defaults `cross_provider: preferred`; full combination matrix; `providers.cross_model: true` alone loads only the `headless` kind. | Side claude accepted; semantics of `none` settled under dispute 2 below (SP-01, SP-09, SP-21, PL-16). |
| A.8 | AGREE | Default probe spends no generation; quota stays `unknown`; a generation rung is a separate, budgeted, configured check. | Side claude accepted (CX-04, CN-13, C070–C078, R-2); merged rule under dispute 1. |
| A.9 | AGREE | A named provider that fails parks the role; fallback only from `auto` or an explicit fallback list; same for transport kinds. | Identical to `RESPONSE-codex.md` dispute 3 (PL-20, C031, C046; R-5). |
| A.10 | AGREE | `send` returns `submission_id`; results carry it; `wait` completes on a matching result file, `blocked`, or timeout; the blocking `wait` call plus watchdog exit is the only wake. | Side claude accepted (HR-07, HR-09, CN-15, R-6); `RESPONSE-codex.md` a.6 and a.7 state the same correlation and waiting-call rule (PL-33, SP-05). |
| A.11 | AGREE | Kinds advertise `messaging_attributed`, `approval_ui`, `controller_state`, `visibility` separately; content in files, approvals never. | Side claude accepted (DT-08, DT-11, C029–C033, C051); no counter-position. |
| A.12 | AGREE | No tier cascade; frontier at every round; planners never synthesize the conversation; referee model sharing recorded, third provider referees when usable. | Side claude accepted (PL-29, PL-32); `RESPONSE-codex.md` a.11 adds planner debate before the grill per the human ruling — compatible, adopted. |
| A.13 | AGREE | Phase 1 = knowledge store only; rendered digests on demand, never committed. | Identical to `RESPONSE-codex.md` dispute 4 and a.9 (KS-10, KS-16–KS-17, C023–C024). |

## 2. Objections in `CRITIQUE-claude.md` §a (raised against side codex; answered in `RESPONSE-codex.md`)

| id | verdict | agreed position | reason |
|---|---|---|---|
| a.1 | AGREE | Non-generation default. Before a hands-off run with cross-provider roles: either a configured bounded generation check runs, or config explicitly accepts unknown readiness, else the run stops at preparation with `unknown_readiness`. | Codex's compromise closes the gap `RESPONSE-claude.md` dispute 1 left open (what a hands-off run does when `providers.probe: live` is unset). SP-07 (no human present), C070–C078, CX-04. |
| a.2 | AGREE | Canonical facts under `{spec-dir}/knowledge/` through one script; no notes-adapter hop. | Codex accepted (KS-03, KS-10, KS-17); matches `RESPONSE-claude.md` dispute 5. |
| a.3 | AGREE | Scoped JSONL events, serialized idempotent writes, on-demand Markdown views. Markdown tables were adequate as a prototype (this round ran on them), not as the store. | Codex's compromise equals side claude's format (KS-05, KS-06, KS-10, PL-40). |
| a.4 | AGREE | Explicit `none` exists beside unset/native. | Codex rejected side claude's original objection; side claude withdrew it in `RESPONSE-claude.md` obj 7 and dispute 2 (BRIEF goal 6, SP-01, SP-09, SP-21). Semantics under dispute 2. |
| a.5 | AGREE | `hierarchy` deferred until one transport proves nesting, delegated ownership, recovery, descendant cleanup. | Both sides (PL-30, CN-02, C019, C032, C045, R-7). |
| a.6 | AGREE | Watchdog exit 3/4 consumed through a waiting call that resumes the manager; `parked(timeout)` recorded; cleanup confirmed separately. | Same as A.10 (PL-33, SP-05, CN-15). |
| a.7 | AGREE | herdr verb mapping from `PROPOSAL-claude.md` §d with submission/result correlation; permission path tested, bypass not assumed. | Codex accepted (HR-01–HR-13, HR-22 scoped); side claude adopts the HR-22 scoping. |
| a.8 | AGREE | Cold-start Claude quota = `unknown`; scoped status-line observations consumed when supplied (QH02, QH09, QH10). | Codex's compromise; `FACTS-quota.md` now documents the status-line feed side claude's critique lacked (C074, CN-12). |
| a.9 | AGREE | Order: knowledge store → herdr planner pair with bounded debate → headless/provider routing → 1DevTool → broader patterns. | Codex moved herdr forward (HR-25); matches `RESPONSE-claude.md` dispute 4 order. |
| a.10 | AGREE | Gate test: byte-identical effective config with new blocks absent, plus zero new probes, processes, installs, external writes — asserted by running the probe with stub `claude`/`codex` executables first on `PATH` that fail loudly if invoked. | Codex accepted and strengthened (PL-10, PL-16); side claude names the check mechanism so it is deliverable. |
| a.11 | AGREE | Review debate inside the settle loop (SP-12, SP-13); planner debate before the grill; convergence completes only on reasoned AGREE or human-only DISAGREE-FINAL. | Codex's compromise restates the human ruling in `../GRILL-LOG.md` and `CONVERGE.md`'s own protocol; side claude adopts it. |
| a.12 | AGREE | Claude background sessions inside the `headless` kind; `claude-team` optional kind; messaging and history tested, exit/cleanup receipts separate. | Codex accepted (CN-02, CN-05, CN-06, C066–C067); matches `PROPOSAL-claude.md` §d kinds. |

## 3. Open disputes

| id | verdict | agreed position | reason |
|---|---|---|---|
| D1 probe | AGREE | Default never generates: binary → login → metadata where documented (C071; C072 inferred until one live call). `providers.probe: live` enables a bounded generation check on the intended provider and model, budgeted for startup context, hooks, output, duration, retries. Hands-off with cross-provider roles: live check, or explicit `accept_unknown_readiness`, or stop at preparation. Cached results are scoped and dated; none promises the next request succeeds (C078). | Both sides reached the same default (CX-04, CN-13, C070–C078, R-2); codex's preparation-stop rule fills side claude's gap. `GRILL-LOG.md`'s auto-mode steer concerns permissions, not this probe. |
| D2 none | AGREE | `none` = no child agents of any kind, including external. Unset/native unchanged. A stage that requires independence (review gate, adversary gate, autopilot) reports `blocked(independence_unavailable)` under `none` and does not run weakened. Shipping the value at all is human D1. | Side claude's `RESPONSE-claude.md` dispute 2 proposed running such stages inline with an `independence: none` stamp; codex's stop rule is the plugin's existing missing-capability behaviour (PL-20) and keeps SP-12 intact. Side claude concedes: stop, do not stamp. |
| D3 fallback | AGREE | Named provider fails → park the role with `provider_unavailable(reason)`. Fallback only from `provider: auto` or an explicit `fallback: [..]` list; each substitution recorded. Same rule for transport kinds. | Identical positions (PL-20, PL-29, C031, C046, R-5). |
| D4 phase 1 | AGREE | Scoped facts + retained evidence, filtered retrieval, rebuildable catalog, artifact and termination receipts, `lost` events for interrupted capture, all around existing native children; the defaults gate test of a.10. No provider or environment probe, transport family, or review routing. Acceptance: close a planner; a fresh agent answers from its rows without repeating research. Phase 2 = herdr planner pair + debate. | Identical scope (KS-03, KS-10, KS-16–KS-17, C023–C024, HR-25); side claude adds the gate test both sides already accepted. |
| D5 store | AGREE | Canonical JSONL under `{spec-dir}/knowledge/`; repo-wide rebuildable catalog of subjects, scoped ids, pointers, no claim copies; views on demand. `/afk:gc` needs no edit: it keeps any file off its delete list (R-3). Evidence that lives under `plan/` is copied into `{spec-dir}/knowledge/evidence/` by the writer at capture time with its hash, so no relocation step at gc; a row whose evidence is still `gone` is unusable without re-derivation. Commit vs gitignore = human question 11 in `PROPOSAL-claude.md` §i. | Codex asked "relocate before cleanup or mark unusable"; capture-time copy satisfies the first, dispute 6's freshness rule the second (KS-16, KS-17, C024). |
| D6 verified | AGREE | A verified conclusion = scoped assertion + checked evidence + an executable assertion or recorded reviewer judgment. `evidence_status` tracked separately; hashes, zero exits, references never promote. Reuse checks scope and expiry; review repeats when supporting conditions change; historical verdicts preserved. | Positions identical (KS-05–KS-07, C071–C078; `LEDGER-FORMAT.md:289`, R-4). |
| D7 hierarchy | AGREE | Deferred; role graph stays extensible; unsupported nesting rejected before execution; admitted after a bounded trial proves delegated ownership, permitted communication, restart recovery, descendant cleanup. | Positions identical (PL-30, CN-02, C019, C032, C044–C045, R-7). |
| D8 usage limits | DISAGREE-FINAL | **Side claude final position.** Plugin promises: (1) *detect* — classify a quota stop only from the provider's own signal (Claude: limit text/exit, QH12–QH13, QH19; Codex: `turn.failed` with `UsageLimitExceeded`, QC05, exact spelling QC11 unverified) and the provider's window fields (Claude status-line `resets_at`, QH02/QH09/QH10; Codex `account/rateLimits/read`, QC02–QC03, live QC06 inferred); any other failure is not quota. (2) *park* — `parked(quota: <provider>, resets_at\|unknown)` in the roster and journal; no model-written summary at park time because the exhausted provider may refuse it (QR07) — agents checkpoint continuously (journal, `plan/DECISIONS.md`, fact receipts) so the saved state already exists. (3) *learn reset* — from the window fields only; absent → `unknown`. (4) *wake* — only through a mechanism that outlives the parked agent: the contact session's one-shot `CronCreate` while that session stays open and idle (QW01–QW04: ≤ 7 days, up to 30 min late; lost on close, restored on resume QW03); an OS task (`schtasks /SC ONCE`, `StartWhenAvailable`, `WakeToRun`, QW07–QW09) only as an opt-in `/afk:setup` row after a bounded conformance test on the machine (QW10 unverified); herdr and 1DevTool provide no timer (QW11–QW14). (5) *resume* — by session id where persistence is `verified` (Claude `-p`/`--bg`, QR01–QR03; Codex non-ephemeral, QC09–QC10), else respawn fresh from disk handoff (QR06) plus the knowledge store; one retry, then park for the human because a reset timestamp proves neither auth nor another bucket (QC12). (6) *re-route* — never for a named provider (D3); for `auto` roles with a fallback list, re-route instead of waiting. **Stop and ask the human:** reset `unknown`; reset beyond the configured wait horizon (default 24 h, matching Claude's own native cap QH15); a weekly-window limit (QH12, QC01); a second quota stop after a resume (QH17 analogue); installing an OS scheduler; any classification the provider signal leaves ambiguous (QH13). The complete sequence is unverified end to end (QR08) and stays a phase-2+ deliverable behind a bounded exhaustion trial. | `RESPONSE-codex.md` carries no position on D8 (written before the dispute existed), so no agreed row can be claimed. Independently of that, two choices are the human's, not evidence-settleable: **how long an unattended run may wait on a provider before a person is told** (the horizon and the weekly-limit rule), and **whether the plugin may register an OS-level scheduled task on the developer's machine**. Everything else in the position is settleable by the named checks: a bounded exhaustion trial per provider (QC11, QR08), a scheduler conformance test (QW10), one live `account/rateLimits/read` (QC06). |

## 4. Summary for the contact

- 25 objections and 7 disputes: all AGREE with reasons; two concessions by side claude (D2 stop-not-stamp; a.1 preparation-stop rule).
- One DISAGREE-FINAL, D8, because the counterpart position is absent and two values are human-only: the unattended wait horizon (with the weekly-limit rule) and OS-scheduler consent.
- Pre-existing human items untouched: D1 (`none` ships?), C5 (auto mode as product default), question 11 of `PROPOSAL-claude.md` §i (commit vs gitignore the store).
