# Rejected extensions: no new concern, no new grades, no counter-search route

> Status: Decided by agent
> Audited: not yet
> Layer: Requirements
> Context ticket: research-enhancement

Three extensions were considered and rejected. No `council` review concern: participants are ordinary concern reviewers and provenance stays in the consult artifacts (PRD PP-4). No `world` or `org` evidence grades: a research-report claim cites as `spec`, an accepted priority in CLAUDE.md as `pattern`; the grade enum in `scripts/lavish/schema.py` is unchanged. No consultation route into investigation counter-search: a second provider running the same enumeration is not a different method per `INVESTIGATION.md`. Source: `MERGED-PLAN.md` §1 A12 (:34), A14 (:36), §2 dispute 6 (:62).
