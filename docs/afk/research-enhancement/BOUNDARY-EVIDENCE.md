# Boundary evidence — B1-B14

Cited grep evidence backing `investigation:` in `.afk/config.yaml`. Read
`INVESTIGATION.md` (boundary catalog) and `CONFIG.md` (`investigation:`
schema) before editing. Commands run from repo root; `Grep` = ripgrep via
tool, `grep`/`find` = Bash tool (no `rg` binary in this shell).

| # | Verdict | Commands | Hits | Cited hits | Proposed config |
|---|---|---|---|---|---|
| B1 | present, generic method sufficient | name/manifest consistency check: `grep -n '"name"' .claude-plugin/plugin.json .codex-plugin/*.json` | n/a | both manifests name `"afk"`, no aliasing | none — default text search covers it |
| B2 | present | `grep -n "^class " **/*.py` | 3 dispatch instances | `skills/afk/mission-control/scripts/mc/server.py:29` `_Handler(SimpleHTTPRequestHandler)`; `skills/afk/mission-control/scripts/mc/vm.py:30,59` `SectionVM`/`Absent`; `adapters/tracker/jira/api.py:192` `_NoRedirect(HTTPRedirectHandler)` | `judgment-only` site `adapters/tracker/jira/api.py` (framework calls `redirect_request` on the subtype, never named at the call site) |
| B3 | present | `hooks/lib/provider.sh` read; `hooks/lib/adapter.sh` read; `grep -n importlib.util scripts/tests/*.py` | provider glob-source + adapter kind-source + name-built function lookup | `hooks/lib/provider.sh:7-9` (`for afk_adapter in .../providers/*.sh; . "$afk_adapter"`) and `:19-33` (`afk_${name}_detect` built then `command -v`); `hooks/lib/adapter.sh:139-146` (`. "$dir/gates.sh"` by config-selected kind) and `:155-160` (`afk_bg_${kind//-/_}_discover` built then `command -v`) | `judgment-only` site `hooks/lib/provider.sh` (provider glob-source + name-to-function lookup); second `judgment-only` site `hooks/lib/adapter.sh` (kind-selected gates.sh source + built discover-fn lookup) |
| B4 | present | manual read of `hooks/lib/provider.sh`, `hooks/lib/adapter.sh`, `.afk/config.yaml` | string keys select provider/kind/adapter | `hooks/lib/provider.sh:19` (`AFK_PROVIDER` env selects by name); `hooks/lib/adapter.sh:127-146` (`build-gates:` kind string selects `adapters/build-gate/<kind>/`); `.afk/config.yaml:7-8` (`tracker: github-issues` selects `adapters/tracker/github-issues/`) | pattern `(AFK_PROVIDER|build-gates:|^tracker:\|^forge:\|^notes:)` over `hooks/lib/*.sh` and `.afk/config.yaml` |
| B5 | present | `Grep "json\.load\|json\.dump" scripts --glob *.py` (17 files); `Grep "\.jsonl" -g *.py -g *.sh -g *.md` (15 files); `grep -n "^import" scripts/afk-config.py` (no `yaml` import) | 17 JSON r/w files, 15 JSONL refs, 1 custom-YAML-subset parser | `scripts/afk-config.py` (own YAML-subset codec for `.afk/config.yaml`, no `yaml` import); `hooks/lesson-append.sh`, `hooks/lesson-digest.sh` (`.claude/lessons/LEDGER.jsonl`); `scripts/lavish_render.py` (round `.json`) | pattern `\.jsonl?\b` over `scripts/` `hooks/`; judgment-only site `scripts/afk-config.py` (hand-rolled YAML-subset reader, not a library codec) |
| B6 | absent | `find . -maxdepth 2 -iname pom.xml -o -iname package.json -o -iname build.gradle*` (0 hits); CLAUDE.md read ("No package build step") | 0 | none | none |
| B7 | absent | same `find` as B6 (0 reactor/aggregator manifests); `.afk/config.yaml` has no `maven`/`build-gates` block | 0 | none | none |
| B8 | present | manual read `hooks/lib/provider.sh`; `.afk/config.yaml` read | env vars + config schema | `hooks/lib/provider.sh:19` `AFK_PROVIDER`; `:59,73` `AFK_PLUGIN_ROOT`/`CLAUDE_PLUGIN_ROOT`/`PLUGIN_ROOT` (via mcp launcher `.mcp.json`); `.afk/config.yaml` (whole file — `schema`, `tracker`, `forge`, `notes`, `git.*`) | pattern `AFK_[A-Z_]+` over `hooks/` `scripts/`; site `.afk/config.yaml` itself (config schema, `scripts/afk-config.py` is the reader) |
| B9 | absent | `Grep "CREATE TABLE\|migration\|\.sql\b" scripts hooks` — only hit is `hooks/wiring-gate.sh:39` excluding `*/db/migration/*` as a path pattern for *consuming* repos, not this repo's own schema | 1 (false positive: path-exclusion glob, not this repo's persistence) | `hooks/wiring-gate.sh:39` | none |
| B10 | present | `Grep "requests\.\|urllib\.request\|http\.client\|urlopen" --glob *.py` (4 files); `Grep "curl \|https://\|http://" hooks` (3 files); `.mcp.json` read; `Grep "gh api\|gh pr\|glab mr\|glab api"` (4 files) | HTTP + MCP stdio + forge CLI callers | `adapters/tracker/jira/api.py` (HTTP client, class `Jira`); `mcp-servers/tracker/server.py` + `.mcp.json:3-10` (stdio MCP server launch); `hooks/update-notice.sh:96` (`curl -fsS` to `raw.githubusercontent.com`); `adapters/forge/github/forge.sh`, `adapters/forge/gitlab/forge.sh` (`gh`/`glab` calls) | pattern `urllib\.request\|requests\.|curl ` over `adapters/` `hooks/`; judgment-only site `mcp-servers/tracker/server.py` (stdio protocol, no literal endpoint string) |
| B11 | absent | `Grep "cron\|scheduled\|EventListener\|on_commit\|queue\." scripts hooks` — hits are all DOM `addEventListener` in `hooks/lavish-tips.sh`/`lavish-dark.sh` (browser JS), no backend listener/queue/scheduler | 13 (all false positives: browser DOM events) | `hooks/lavish-tips.sh:443` etc. (JS `addEventListener`, not a backend async boundary) | none |
| B12 | present, generic method sufficient | `ls scripts/tests \| wc -l` (26 files); tests also nest per-skill (`skills/*/scripts/tests/`) | 26+ files | `scripts/tests/` (26 files); `skills/afk/mission-control/scripts/tests/test_contract.py` | none — default "search name forms under test sources" already spans every `scripts/tests/` and `skills/*/scripts/tests/` dir |
| B13 | present, generic method sufficient | manual: `CLAUDE.md`, `SKILL.md` tree, `adr/`, `docs/` all markdown asserting behaviour | pervasive | `CLAUDE.md` (this repo's own steering doc); `INVESTIGATION.md`, `CONFIG.md`, `adr/000*.md` | none — default markdown name-form search covers it |
| B14 | present (frontier/unverified by definition, never absence) | `Grep "gh issue\|gh api\|gh pr\|glab mr\|glab api" .` (4 files); `hooks/update-notice.sh` read (GitHub release check) | 4 forge/tracker CLI sites + 1 release-check site | `adapters/forge/github/forge.sh`, `adapters/forge/gitlab/forge.sh` (live forge repos); `adapters/tracker/github-issues/api.py`, `skills/utils/report-issue/scripts/publish.sh` (live tracker/plugin-issue repo); `hooks/update-notice.sh:93-96` (this plugin's own GitHub repo, release-check) | judgment-only site `hooks/update-notice.sh` (external repo: `midnightblur/afk-driver` releases) plus judgment-only site `adapters/forge/*`, `adapters/tracker/github-issues/*` (external forge/tracker repos, consuming-repo-selected) |
