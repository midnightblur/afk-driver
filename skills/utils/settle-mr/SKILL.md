---
name: settle-mr
description: Deprecated alias for settle-change. Review a forge change request (GitLab MR, GitHub PR) and settle it through the review settle loop, using the change itself as the ledger. Use when the user drops an MR/PR URL or IID for review, wants one made ready, or asks to re-check one after fixes landed.
---

> **Language:** read `LANGUAGE.md` (plugin root) first — it binds every word this skill produces.

# settle-mr — compatibility alias

Print this line once, then continue:

> `/afk:settle-mr` is the old name. The skill is now `/afk:settle-change`. The alias is removed in the next major version.

Then read `skills/utils/settle-change/SKILL.md` from the plugin root and follow it verbatim with the same arguments. Add nothing of your own; this file holds no behaviour.
