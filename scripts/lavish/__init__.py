"""Deterministic renderer for lavish decision surfaces (the kit path).

`LAVISH-KIT.md` at the plugin root is the normative contract: the round JSON,
the six components, the skeleton and the response grammar. This package only
implements it. Standard library only, no network, no generated-and-committed
output — the HTML is a build product of the JSON.
"""
